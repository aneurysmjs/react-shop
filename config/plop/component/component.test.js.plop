import React from 'react';
import { render } from '@testing-library/react';

import {{pascalCase componentName}} from './{{pascalCase componentName}}';

describe('{{pascalCase componentName}}', () => {
  it('should have component\'s name as className', () => {
    const { container } = render(<{{pascalCase componentName}} />);
    const div = container.firstChild;
    expect(div.className).toEqual('{{camelCase componentName}}');
  });
});